name: testing

on: push

jobs:
  test:
    runs-on: ubuntu-18.04
    steps:
      - name: Test container
        uses: rectalogic/test-actions/.github/actions/myaction@master
        with:
          myInput: hello
